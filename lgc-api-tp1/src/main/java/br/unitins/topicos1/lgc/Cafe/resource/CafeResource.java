package br.unitins.topicos1.lgc.Cafe.resource;

import jakarta.ws.rs.Consumes;
import jakarta.ws.rs.Path;
import jakarta.ws.rs.Produces;
import jakarta.ws.rs.core.MediaType;

@Path("/cafes")
@Produces(MediaType.APPLICATION_JSON)
@Consumes(MediaType.APPLICATION_JSON)
public class CafeResource {
    
    //  preencher com GET / POST / PUT / DELETE         [ C.R.U.D ]

    /*
     *  GET     :   Buscar/Trazer em uma pesquisa sobre o objeto(Buscar por todos[listar], buscar por nome, buscar por caracteristicas buscáveis)
     * 
     *  POST    :   Criar o objeto
     * 
     *  PUT     :   Atualizar os dados do objeto
     *  
     *  DELETE  :   Deletar os dados do objeto/Deletar o objeto
     * 
     * 
     * 
     */

}
